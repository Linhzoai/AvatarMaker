name: Deploy Vite React App to GitHub Pages

on:
  push:
    branches: [main]  # 👈 Tự động deploy mỗi khi push code lên nhánh main

permissions:
  contents: write  # Cho phép ghi nội dung vào repo (để tạo nhánh gh-pages)

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout code
        uses: actions/checkout@v3

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🏗️ Build project
        run: npm run build

      - name: 🚀 Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist   # 👈 Thư mục build đầu ra của Vite
